-# in app/views/ideas/show.html.haml
%div.centralIdea
  = form_tag idea_path(@idea), :method => :put do

    = text_field :idea, 'title'

    %ul#details
      %li
        Created By:
        = link_to "#{@user.name}", user_path(User.find_by_id(@user.id))

      %li
        Points:
        = @idea.rank

    %h3 Description:

    = text_area :idea, 'description', :size => '30x7', :id => 'description' 
    Make Private
    = check_box_tag "Make Private", 1, false, :id=>"privacy"

    =submit_tag 'Save and Post'

  / %table#collaborators
  /   %thead
  /     %tr
  /       %th Collaborators
  /   %tbody
  /     - @idea.collaborators.each do |user|
  /     %tr
  /       %td= user.name
  / %h1 here 

  =#Colin added 4/18 for adding collaborators:
  =form_tag idea_path(@idea), :method => :put do
    =label :idea, :collaborator, "Add Collaborator:"
    =text_field_tag :collaborator, 'Enter username'#, :collaborators<<
    =submit_tag 'Add Collaborator'

  %header
    -if @current_user==@user.id
      = link_to 'Edit', edit_idea_path(@idea)
      = button_to 'Delete', idea_path(@idea), :method => :delete, :confirm => 'Are you sure?'
    -else
      = link_to 'home', root_path


= link_to 'Back to idea list', ideas_path